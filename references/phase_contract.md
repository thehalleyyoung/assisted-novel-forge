# Phase contract

Each prescribed member must:

- Read the pack root (`brief.json`, `voice_contract.md`, `forge_state.json` when present)
- Write only its declared artifacts (world/, plot/, chapters/, review/, continuity/)
- Never claim manuscript done — `verify_gate.py` is sole done authority

`compose.py` may omit a member entirely if its pressure is already low.
