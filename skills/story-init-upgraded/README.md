# story-init-upgraded

Upgraded fork of `danjdewhurst/story-skills@story-init` — middling marketplace skill made substantially more useful.

## Install (Claude + Cursor)

```bash
git clone <repo-url>
cd story-init-upgraded
bash install.sh story-init-upgraded
```

## Use (after install)

```text
/story-init-upgraded <describe your task>
```

## Upstream

See `UPSTREAM.md`. Upgrade plan: `UPGRADE_PLAN.md`. Forge log: `USEFULNESS_FORGE.md`.
