# humanize-upgraded

Upgraded fork of `humanizerai/agent-skills@humanize` — middling marketplace skill made substantially more useful.

## Install (Claude + Cursor)

```bash
git clone <repo-url>
cd humanize-upgraded
bash install.sh humanize-upgraded
```

## Use (after install)

```text
/humanize-upgraded <describe your task>
```

## Upstream

See `UPSTREAM.md`. Upgrade plan: `UPGRADE_PLAN.md`. Forge log: `USEFULNESS_FORGE.md`.
