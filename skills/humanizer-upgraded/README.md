# humanizer-upgraded

Upgraded fork of `cowork-os/cowork-os@humanizer` — middling marketplace skill made substantially more useful.

## Install (Claude + Cursor)

```bash
git clone <repo-url>
cd humanizer-upgraded
bash install.sh humanizer-upgraded
```

## Use (after install)

```text
/humanizer-upgraded <describe your task>
```

## Upstream

See `UPSTREAM.md`. Upgrade plan: `UPGRADE_PLAN.md`. Forge log: `USEFULNESS_FORGE.md`.
