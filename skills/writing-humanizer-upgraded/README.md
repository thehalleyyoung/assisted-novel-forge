# writing-humanizer-upgraded

Upgraded fork of `hairyf/skills@writing-humanizer` — middling marketplace skill made substantially more useful.

## Install (Claude + Cursor)

```bash
git clone <repo-url>
cd writing-humanizer-upgraded
bash install.sh writing-humanizer-upgraded
```

## Use (after install)

```text
/writing-humanizer-upgraded <describe your task>
```

## Upstream

See `UPSTREAM.md`. Upgrade plan: `UPGRADE_PLAN.md`. Forge log: `USEFULNESS_FORGE.md`.
